master node -> slave nodes

pods : stores containers, k8s manages pods and not containers
it is the smallest component of a cluster 

namespaces : group
it can be create using sh command : kubectl create ns <name>
or 
by using yml file 

load ballancer: acts as a manager of a parking lot 

pushing 

apiVersion
kind
metadata
    name
    labels


ingress : ip to domain converter
push

